<?php

declare(strict_types=1);

/*
| The Minimax MCP server is registered as the local server "minimax" by
| MinimaxServiceProvider — start it with `php artisan mcp:start minimax`.
|
| To ALSO expose it over HTTP for remote AI clients, uncomment the line below
| (protect it with auth middleware). Publish this file with:
|   php artisan vendor:publish --tag=minimax-ai-routes
*/

// use Laravel\Mcp\Facades\Mcp;
// use Nejcc\Minimax\Mcp\MinimaxServer;
//
// Mcp::web('/mcp/minimax', MinimaxServer::class)->middleware('auth:sanctum');
